// Reflux actions for the application

var Reflux = require("reflux");

var actions = {};

// Helpers to define actions with less typing
var action = (name, options) => actions[name] = Reflux.createAction(options);

/////////////////////////////
// Sync action definitions //
/////////////////////////////

sync("priceUpdate", {})

//////////////////////////////
// Async action definitions //
//////////////////////////////

async()

module.exports = actions;
